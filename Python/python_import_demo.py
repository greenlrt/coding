import python_import

print(python_import.test)

python_import.printHello()

# module that is imported must be in same directory as the file the module is
# being imported in.

# import module: imports the module. must reference module name when getting
# object from module.

# from module import *: imports everything in the module. reference to module
# when getting object will result in an error.

# from module import object: only imports object in the module.
